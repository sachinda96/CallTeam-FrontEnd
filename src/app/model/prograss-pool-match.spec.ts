import { ProgressPoolMatch } from './progress-pool-match';

describe('PrograssPoolMatch', () => {
  it('should create an instance', () => {
    expect(new ProgressPoolMatch()).toBeTruthy();
  });
});
